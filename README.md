1. เช็ค version ของ CUDA ที่ติดตั้ง: พิมพ์ nvcc --version ใน Command Prompt
2. ตรวจสอบ PyTorch ที่ติดตั้งโดยพิมพ์คำสั่งนี้ใน terminal เพื่อดูเวอร์ชันที่ใช้งานอยู่
  import torch
  print(torch.__version__)
